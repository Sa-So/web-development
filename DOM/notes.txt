add js to website
add css at top // so it applies style before js ?
add js at bottom // because if we can't change an element before we read its html and render it on page // also lets site content load faster than unnecessary things like ads
